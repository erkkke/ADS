#include <bits/stdc++.h>
#define ll long long
using namespace std;



int main(){
    int t; cin >> t;
    int cnt = 0;
    for (int i = 0; i < t; i++){
        int n, m, k, w;
        int a[n], b[m];
        cin >> n >> m >> k >> w;
        for (int j; j < n; j++)
            cin >> a[j];
        for (int j; j < m; j++)
            cin >> b[j];
        sort(a, sizeof(a) / sizeof(a[0]));
        sort(b, sizeof(b) / sizeof(b[0]));
        int l1 = 0, l2 = 0;
        while (a[l1] < w) {
            
        }
        

    }
}